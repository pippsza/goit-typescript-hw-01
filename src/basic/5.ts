enum DayOfWeek {
  Monday = true,
  Tuesday = true,
  Wednesday = true,
  Thursday = true,
  Friday = true,
  Saturday = false,
  Sunday = false,
}

const isWeekend = (day: boolean): boolean => {
  return day;
};
